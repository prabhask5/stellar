{
  "name": "stellar-focus-extension",
  "version": "1.0.6",
  "description": "Browser extension to block distracting websites during Stellar focus sessions",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:firefox": "node build.js firefox",
    "build:chrome": "node build.js chrome",
    "clean": "rm -rf dist-firefox dist-chrome",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && cd dist-firefox && zip -r ../stellar-focus-firefox.zip . && cd ../dist-chrome && zip -r ../stellar-focus-chrome.zip . && cd ..",
    "release": "node release.js"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.10.7",
    "esbuild": "^0.27.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "webextension-polyfill": "^0.12.0"
  }
}
